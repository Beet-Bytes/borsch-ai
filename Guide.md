# 🥣 Стартовий Гайд: Початок роботи над AI Borsch

Вітаємо в команді! 🎉
Це ваш перший проєкт, тому ми зробили цей гайд максимально детальним. Ваша мета зараз — налаштувати свій комп'ютер так, щоб сайт AI Borsch запустився у вас локально.

Ми використовуємо технологію Docker. Це означає, що вам не треба встановлювати складні бази даних вручну. Ви натискаєте одну кнопку — і все працює саме.

Дотримуйтесь кроків послідовно. 👇

## 🛠 Етап 0: Встановлення програм (Зробити 1 раз)

Перед тим як качати код, встановіть ці 4 програми. Без них нічого не вийде.

VS Code (Редактор коду)

Завантажити: code.visualstudio.com

Порада: Це наш основний інструмент. Встановіть його першим.

Git (Система контролю версій)

Завантажити: git-scm.com

При встановленні: Просто тисніть "Next" на всіх екранах.

Docker Desktop (Головна програма для запуску)

Завантажити: docker.com/products/docker-desktop

ВАЖЛИВО: Після встановлення запустіть Docker Desktop і зачекайте, поки внизу зліва загориться зелена смужка (Engine running). Він має працювати у фоні завжди, коли ви працюєте над проєктом.

Python (Потрібен для перевірки якості коду)

Завантажити: python.org

Windows: Обов'язково поставте галочку "Add Python to PATH" при встановленні!

## 📥 Етап 1: Скачування та Налаштування проєкту

Тепер, коли софт є, давайте налаштуємо сам проєкт.

1. Клонування репозиторію
   Відкрийте термінал (або краще відкрийте VS Code, і там у меню Terminal -> New Terminal). Введіть:

```bash
git clone https://github.com/Beet-Bytes/borsch-ai.git
cd borsch-ai
```

2. Налаштування "Секретів" (.env)
   Проєкт не запуститься без паролів до бази даних. Ми підготували файл-приклад. Вам треба скопіювати його.

У VS Code: Знайдіть зліва файл .env.example.

Скопіюйте його вміст.

Створіть новий файл поруч і назвіть його просто .env (крапка env).

Вставте туди скопійований текст. Збережіть.

3. Налаштування "Контролера якості" (Pre-commit)
   Ми використовуємо інструмент, який не дасть вам відправити погано відформатований код. Він працює на вашому комп'ютері (не в Докері), тому треба налаштувати Python локально.

В терміналі VS Code виконайте (рядок за рядком):

```bash
# 1. Створюємо ізольоване середовище
python -m venv .venv

# 2. Активуємо його (зверніть увагу на різні команди для Windows і Mac!)
# Windows (PowerShell/CMD):
.venv\Scripts\activate

# Mac/Linux:
source .venv/bin/activate

# (Якщо з'явився напис (.venv) на початку рядка — ви все зробили вірно!)

# 3. Встановлюємо pre-commit
pip install pre-commit
pre-commit install
```

Тепер, коли ви будете робити git commit, система сама перевірить ваші файли.

## 🚀 Етап 2: Перший запуск
Створіть файл .env та заповніть його по прикладу .env.example.
Переконайтеся, що програма Docker Desktop запущена (ви бачите іконку кита біля годинника).

В терміналі (знаходячись у папці проєкту) введіть:

```bash
docker-compose up --build
```

Що зараз відбувається?
В терміналі побіжать літери. Docker сам скачує Базу Даних (MongoDB), сам налаштовує Python (Backend) і Next.js (Frontend). Перший раз це може зайняти 5-10 хвилин.

Коли побачите написи, що сервери запущені — вітаю! Ви розгорнули проєкт.

Де дивитися результат:

🌍 Сайт (Frontend): http://localhost:3000

⚙️ API (Backend Docs): http://localhost:8000/docs

🗄️ База Даних (Admin UI): http://localhost:8081

(Щоб зупинити сервер — натисніть Ctrl+C у терміналі).

## 🔄 Етап 3: Як працювати щодня (Workflow)

Тепер, коли все налаштовано, ваш робочий день виглядає так. Ми працюємо за схемою: Task -> Branch -> Code -> PR.

1. Початок роботи
   Відкрийте Jira, перетягніть свою задачу в "In Progress". Запам'ятайте номер (наприклад, BH6-15).

Відкрийте VS Code і термінал.

Оновіть головну гілку і створіть свою:

```bash
git checkout develop       # Переходимо на головну гілку розробки
git pull                   # Скачуємо зміни колег
git checkout -b feature/BH6-15-login-page  # Створюємо свою гілку
```

Запустіть проєкт:

```bash
docker-compose up
```

2. Написання коду
   Пишіть код. Сайт на localhost:3000 буде оновлюватися автоматично (Hot Reload).
   Якщо ви додали нову бібліотеку в Python/JS, треба перезапустити Docker: Ctrl+C -> docker-compose up --build.

3. Збереження (Commit & Push)
   Коли задача готова:

Відкрийте ще один термінал (плюсик у VS Code), щоб не зупиняти сервер, або зупиніть сервер (Ctrl+C).

Важливо: Переконайтеся, що віртуальне середовище активоване (горить (.venv)). Якщо ні — активуйте знову (.venv\Scripts\activate).

Команди:

```bash
git add .
git commit -m "BH6-15: Added login form layout"
# Якщо pre-commit знайшов помилки — він їх виправить. Просто повторіть add і commit.

git push origin feature/BH6-15-login-page
```

4. Здача роботи (Pull Request)
   Йдіть на сторінку GitHub репозиторію.

Там з'явиться жовта плашка "Compare & pull request". Натисніть її.

Переконайтеся, що base: develop, а compare: ваша-гілка.

Натисніть Create Pull Request.

Киньте посилання тімліду на перевірку.

## ❓ FAQ: Що робити, якщо не працює?

Q: Docker пише port is already allocated.
A: У вас вже щось запущено. Можливо, інший екземпляр проєкту в іншому терміналі? Або локально встановлена Mongo? Вбийте всі процеси або перезавантажте ПК.

Q: git commit видає помилки і не зберігає.
A: Це наш захист. Читайте, що він пише. Зазвичай він каже "Fixed files". Це означає, що він поправив коми/відступи за вас. Просто зробіть git add . і git commit ... ще раз.

Q: Я не можу активувати venv у Windows, пише "running scripts is disabled".
A: Відкрийте PowerShell від імені адміністратора і введіть: Set-ExecutionPolicy RemoteSigned, натисніть Y. Потім спробуйте знову.
